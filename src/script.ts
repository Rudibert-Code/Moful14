// .ts = TypeScript

let add = (a:number, b:number) => {return a + b};

// Konsoleneintrag zum Umwandeln in modernes JavaScript : tsc script.ts --target ESNext --module commonjs 
// Konsoleneintrag zum Erzeugen einer tsconfog.json : tsc --init